image: "python:3.6"

before_script:
  - apt update
  - python --version
  - apt -y install python-pip
  - python -m pip install -r requirements.txt

stages:
  - test

test:
  stage: test
  script: python py.test

